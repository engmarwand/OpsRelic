{
  "entities": {
    "User": {
      "title": "User Profile",
      "description": "User account information and subscription tier",
      "type": "object",
      "properties": {
        "plan": {
          "type": "string",
          "enum": ["starter", "pro", "agency"],
          "description": "Subscription tier of the user"
        }
      },
      "required": ["plan"]
    },
    "Workspace": {
      "title": "Workspace Settings",
      "description": "Agency branding and configuration",
      "type": "object",
      "properties": {
        "brand": {
          "type": "object",
          "properties": {
            "name": { "type": "string" },
            "tagline": { "type": "string" },
            "logoUrl": { "type": ["string", "null"] }
          }
        },
        "color": {
          "type": "object",
          "properties": {
            "primary": { "type": "string" }
          }
        },
        "layout": {
          "type": "object",
          "properties": {
            "theme": { "type": "string" },
            "layout": { "type": "string" }
          }
        },
        "notifications": {
          "type": "object"
        }
      }
    },
    "ClientAccount": {
      "title": "ClientAccount",
      "description": "Represents a client or prospect in the CRM pipeline",
      "type": "object",
      "properties": {
        "name": { "type": "string" },
        "email": { "type": "string" },
        "website": { "type": "string" },
        "stage": { "type": "string", "enum": ["Lead", "Qualified", "Proposal", "Won/Onboarding", "Active Client"] },
        "notes": { "type": "string" },
        "nextStep": { "type": "string" },
        "userId": { "type": "string" },
        "createdAt": { "type": "string", "format": "date-time" },
        "updatedAt": { "type": "string", "format": "date-time" }
      },
      "required": ["name", "stage", "userId"]
    },
    "Campaign": {
      "title": "Campaign",
      "description": "Tracking campaign for agency projects",
      "type": "object",
      "properties": {
        "name": { "type": "string" },
        "userId": { "type": "string" },
        "clientId": { "type": "string" },
        "status": { "type": "string", "enum": ["Draft", "Review", "Active", "Complete"] },
        "intake": { "type": "object" },
        "brief": { 
          "type": "object",
          "properties": {
            "summary": { "type": "string" },
            "objective": { "type": "string" },
            "angle": { "type": "string" },
            "nextSteps": { "type": "string" },
            "platforms": { "type": "array", "items": { "type": "string" } }
          }
        },
        "createdAt": { "type": "string", "format": "date-time" },
        "updatedAt": { "type": "string", "format": "date-time" },
        "budget": { "type": "number" },
        "portalPassword": { "type": "string" }
      },
      "required": ["name", "userId", "status"]
    },
    "Submission": {
      "title": "Submission",
      "description": "Content asset submissions from creators",
      "type": "object",
      "properties": {
        "userId": { "type": "string" },
        "campaignId": { "type": "string" },
        "title": { "type": "string" },
        "views": { "type": "number" },
        "payout": { "type": "number" },
        "status": { "type": "string" },
        "platform": { "type": "string" },
        "url": { "type": "string" }
      },
      "required": ["userId", "campaignId", "title"]
    },
    "UserConfig": {
      "title": "User Configuration",
      "description": "Creator pipeline, guidelines and message templates",
      "type": "object",
      "properties": {
        "invites": { "type": "array" },
        "guidelines": { "type": "array" },
        "templates": { "type": "array" }
      }
    }
  },
  "firestore": {
    "/users/{userId}": {
      "schema": "User",
      "description": "Individual user data"
    },
    "/workspaces/{userId}": {
      "schema": "Workspace",
      "description": "Agency workspace configuration"
    },
    "/campaigns/{campaignId}": {
      "schema": "Campaign",
      "description": "Campaign documents"
    },
    "/clients/{clientId}": {
      "schema": "ClientAccount",
      "description": "Client and relationship documents"
    },
    "/submissions/{submissionId}": {
      "schema": "Submission",
      "description": "Content submission documents"
    },
    "/user_config/{userId}": {
      "schema": "UserConfig",
      "description": "Pipeline and CRM configuration for a user"
    }
  }
}
