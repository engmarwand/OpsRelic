{
  "entities": {
    "User": {
      "title": "User Profile",
      "description": "User account information and subscription tier",
      "type": "object",
      "properties": {
        "plan": {
          "type": "string",
          "enum": ["starter", "pro", "agency"],
          "description": "Subscription tier of the user"
        }
      },
      "required": ["plan"]
    },
    "Workspace": {
      "title": "Workspace Settings",
      "description": "Agency branding and configuration",
      "type": "object",
      "properties": {
        "brand": {
          "type": "object",
          "properties": {
            "name": { "type": "string" },
            "tagline": { "type": "string" },
            "logoUrl": { "type": ["string", "null"] }
          }
        },
        "color": {
          "type": "object",
          "properties": {
            "primary": { "type": "string" }
          }
        },
        "layout": {
          "type": "object",
          "properties": {
            "theme": { "type": "string" },
            "layout": { "type": "string" }
          }
        },
        "notifications": {
          "type": "object"
        }
      }
    },
    "Campaign": {
      "title": "Campaign",
      "description": "Tracking campaign for agency projects",
      "type": "object",
      "properties": {
        "name": { "type": "string" },
        "userId": { "type": "string" },
        "createdAt": { "type": "string", "format": "date-time" }
      },
      "required": ["name", "userId"]
    },
    "Submission": {
      "title": "Submission",
      "description": "Content asset submissions from creators",
      "type": "object",
      "properties": {
        "userId": { "type": "string" },
        "campaignId": { "type": "string" },
        "title": { "type": "string" },
        "views": { "type": "number" },
        "payout": { "type": "number" },
        "status": { "type": "string" },
        "platform": { "type": "string" },
        "url": { "type": "string" }
      },
      "required": ["userId", "campaignId", "title"]
    },
    "UserConfig": {
      "title": "User Configuration",
      "description": "Creator pipeline, guidelines and message templates",
      "type": "object",
      "properties": {
        "invites": { "type": "array" },
        "guidelines": { "type": "array" },
        "templates": { "type": "array" }
      }
    }
  },
  "firestore": {
    "/users/{userId}": {
      "schema": "User",
      "description": "Individual user data"
    },
    "/workspaces/{userId}": {
      "schema": "Workspace",
      "description": "Agency workspace configuration"
    },
    "/campaigns/{campaignId}": {
      "schema": "Campaign",
      "description": "Campaign documents"
    },
    "/submissions/{submissionId}": {
      "schema": "Submission",
      "description": "Content submission documents"
    },
    "/user_config/{userId}": {
      "schema": "UserConfig",
      "description": "Pipeline and CRM configuration for a user"
    }
  }
}
